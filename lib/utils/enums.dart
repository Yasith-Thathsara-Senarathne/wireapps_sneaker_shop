// variables

// device measure type
enum DeviceMeasureType {
  width,
  height,
  none,
}

// currency type
enum CurrencyType {
  gbp,
  usd,
  none,
}

// shoe size type
enum ShoeSizeType {
  eight,
  nine,
  ten,
  eleven,
  none,
}

// shoe color type
enum ShoeColorType {
  blue,
  black,
  purple,
  green,
  yellow,
  multiColoured,
  none,
}

// remote response type
enum RemoteResponseType {
  noInternet,
  emptyData,
  failed,
  success,
  none,
}
